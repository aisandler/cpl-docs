# My Docs

This site renders Markdown files with a simple, readable style.

## Add or organize pages

- Create new `.md` files in `docs/`
- Add them to the sidebar in `_sidebar.md`
- Commit + push to `main`
